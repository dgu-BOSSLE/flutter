import 'package:flutter/material.dart';

class PreviewScreenSettingsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Preview Screen Settings Screen'),
      ),
      body: Center(
        child: Text('Preview Screen Settings Screen'),
      ),
    );
  }
}
